import React from "react";

const UserContext = React.createContext();

export default UserContext;


// we just create a context by using the inbuilt react function React.createContext() and we export it